% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phreg.R
\name{robust.basehaz.phreg}
\alias{robust.basehaz.phreg}
\alias{summarybase.phreg}
\alias{conftype}
\title{Robust Baseline Hazard Standard Errors}
\usage{
robust.basehaz.phreg(x, type = "robust", fixbeta = NULL, ...)

summarybase.phreg(object, robust = FALSE, ...)

conftype(
  x,
  std.err,
  conf.type = c("log", "plain"),
  restrict = c("positive", "prob", "none"),
  conf.int = 0.95
)
}
\arguments{
\item{x}{point estimate(s).}

\item{type}{type of standard error (default \code{"robust"}).}

\item{fixbeta}{if non-NULL, fixes beta at given value.}

\item{...}{additional arguments.}

\item{object}{a \code{phreg} object.}

\item{robust}{logical; if TRUE, uses robust standard errors.}

\item{std.err}{standard error(s).}

\item{conf.type}{type of transformation: \code{"log"} or \code{"plain"}.}

\item{restrict}{restriction: \code{"positive"}, \code{"prob"}, or \code{"none"}.}

\item{conf.int}{confidence level (default 0.95).}
}
\value{
A list with \code{cumhaz}, \code{se.cumhaz}, and \code{strata}.

An object of class \code{"summary.recurrent"}.

A list with \code{upper}, \code{lower}, \code{conf.type}, and \code{conf.int}.
}
\description{
Computes robust (sandwich) standard errors for the cumulative baseline
hazard from a \code{phreg} object.

Summarizes cumulative baseline hazard estimates from a phreg object,
optionally with robust standard errors.

Computes confidence intervals using log or plain transformations,
with optional restrictions to positive values or probability scale.
}
